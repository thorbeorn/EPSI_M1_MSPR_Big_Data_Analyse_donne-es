macOS / Linux
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirment.txt
python main.py

Windows
python3 -m venv .venv
.venv\Scripts\Activate
pip install -r requirment.txt
python main.py